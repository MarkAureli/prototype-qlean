import Qlean.Core.Basic

/-!
# Qlean.Core

The curated, general-interest core library: definitions and results everyone
builds on. Maintainer-owned (`Qlean/Core/**`), separately versioned, and the
only part external users need to depend on. Core never imports `Qlean.Contrib`.
-/
